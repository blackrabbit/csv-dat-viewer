// The ID to grab the data sources pane ID.
export const DATA_SOURCES_PANE_ID = "data-sources-pane";

// Vertical header footer margin in pixels.
export const VERTICAL_HEADER_FOOTER_MARGIN = 145;

// Left hand side column width in pixels
export const LEFT_HAND_SIDE_COLUMN = 150;

// Padding around table in pixels.
export const PADDING_PIXELS_MARGIN = 60;

// Row height for table in pixels.
export const ROW_HEIGHT_PX = 30;

// Summary view bar height in pixels.
export const SUMMARY_VIEW_BAR_HEIGHT = 200;
